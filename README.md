# Collidoscope
A Python Turtle Graphics program that generates abstract geometric patterns using random parameters. The turtle draws a dynamic spiral-like design from the center while the randomly generated values controlling the pattern are displayed in the top-right corner of the screen.
